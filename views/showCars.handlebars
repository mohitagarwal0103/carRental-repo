{{!-- <html>
  <head>
    
  </head>
  <body>
    {{>navbar}}
    <div class="filter_container">
  <div class="filter_form">
     
                <label class= " filter_label" for="Model">Model:</label>
                <select id="Model">
                    <option value="1">BMW</option>
                    <option value="2">Audi</option>
                    <option value="3">Scoda</option>
                </select>

                <label class= " filter_label"for="Make">Make:</label>
                <select id="Make">
                    <option value="1">2023</option>
                    <option value="2">2022</option>
                    <option value="3">2021</option>
                </select>

    </div>
</div>

    <script>

    </script>

  </body>
</html> --}}

 {{>filter}} 

{{#each cars}} 
   {{!-- {{#carFilter this ../filters}}
            <li>Model: {{model}}, Color: {{color}}</li>
        {{/carFilter}} --}}
  <div class="media row" >
   
     <div class="col-sm">
        <input type="hidden" id="carID" value="{{_id}}">
      <img class="mr-3" src="/image/logo.png" width="100px" height="100px">
     </div>

     <div class="col-sm-7">
        <div class="media-body">
         <a href="/displayCar/{{_id}}"><h5 class="mt-0">{{make}} {{model}} {{year}} </h5></a>   
        <div class="row">
            {{#each image}}
            <div class="col-sm">
                <img src="{{imageUrl}}" class="img-fluid" alt="">
            </div>
            {{else}}
            <img src="/image/images.png" alt="" height="100px" width="100px">
            {{/each}}
        </div>
        <p>${{pricePerHour}} per hour</p>
        <p>${{pricePerWeek}} per week</p>
         <p>{{owner.firstname}} </p> 
         <small>location: {{location}}. </small> 
         <small>{{date}} </small>
        
      </div> 
     </div>

    <div class="col-sm"></div>
</div> 


 <hr> 
{{else}}
<p>No cars found</p>
{{/each}} 
